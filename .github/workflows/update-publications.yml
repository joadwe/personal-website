name: Update Publications from Scholar Export

on:
  workflow_dispatch:

jobs:
  export-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Scholar updates are exported externally
        run: |
          echo "Google Scholar scraping now runs in the local google-scholar Docker app."
          echo "Export docs/data/scholar-dashboard.json from that app, commit it, and let the MkDocs deploy workflow publish the static site."
